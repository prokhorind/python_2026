# 💸 Personal Finance Assistant  
## «Куди зникають мої гроші?»

---

## 📖 Опис завдання

Ви ведете облік своїх витрат у **Excel**, але таблиця сама по собі не відповідає на важливі питання:

- скільки грошей витрачено загалом?
- на що йде найбільше коштів?
- чи не перевищені розумні межі витрат?

У цьому завданні ви використаєте **Python як помічника**, який проаналізує Excel-файл і покаже реальну картину ваших витрат.

> Це не завдання з «програмування заради програмування».  
> Це приклад того, як код може бути корисним у повсякденному житті.

---

## 📁 Вхідні дані

Вам надано файл **`expenses.xlsx`** з одним аркушем, який містить такі стовпці:

| Назва стовпця | Опис |
|--------------|------|
| `day`        | День тижня |
| `category`   | Категорія витрат |
| `amount`     | Сума витрат |

❌ Заборонено змінювати дані вручну під час аналізу  
✅ Дані потрібно обробляти **тільки за допомогою Python-коду**

---

## 🎯 Завдання

### 1️⃣ Зчитування даних
Підключіть Excel-файл `expenses.xlsx` до Python та зчитайте всі витрати.

---

### 2️⃣ Аналіз витрат
Напишіть програму, яка:

1. Обчислює **загальну суму витрат**
2. Підраховує витрати **по кожній категорії**
3. Визначає категорію, на яку витрачено **найбільше коштів**
4. Якщо загальні витрати **перевищують 800 грн**, виводить попередження

---

### 3️⃣ Результат роботи програми
Програма повинна вивести у консоль:
- загальну суму витрат
- категорію з найбільшими витратами
- короткий висновок (норма / перевищення ліміту)

---

## 🔧 Як підключити Excel до Python

### 📦 Встановлення бібліотек (один раз)
```bash
pip install pandas openpyxl
```

# 📊 Створення діаграми в Excel за допомогою Python

У цьому етапі завдання ви автоматизуєте створення **фінансового звіту з діаграмою**. Python не лише аналізує дані, а й формує готовий Excel-файл, який можна одразу відкрити, надіслати або використати для презентації.

---

## 🎯 Мета

* створити **новий Excel-файл** з результатами аналізу;
* додати **стовпчикову діаграму** з витратами за категоріями;
* показати, як Python може замінити ручну роботу в Excel.

---

## 📦 Інструменти

Для цього етапу використовуються:

* **pandas** — для читання та обробки даних;
* **openpyxl** — для роботи з Excel-файлами та створення діаграм.

> 💡 Важливо: бібліотека `openpyxl` дозволяє створювати діаграми прямо всередині Excel-файлу без використання графіків у Python.

---

## 🧠 Загальна логіка роботи

1. Зчитати Excel-файл з витратами.
2. Порахувати загальну суму витрат для кожної категорії.
3. Записати результати у новий Excel-файл.
4. Додати до цього файлу діаграму.

---

## 📈 Як створюється діаграма (пояснення кроків)

### 1️⃣ Підготовка даних

У новому Excel-файлі має бути таблиця такого вигляду:

| category      | amount |
| ------------- | ------ |
| Food          | 1250   |
| Transport     | 420    |
| Entertainment | 980    |

Ці дані використовуються для побудови діаграми.

---

### 2️⃣ Створення діаграми

* використовується **стовпчикова діаграма** (Bar Chart);
* по осі **X** — назви категорій;
* по осі **Y** — загальна сума витрат.

Діаграма автоматично оновлюється, якщо змінюються дані.

---

### 3️⃣ Додавання діаграми до Excel

Python:

* відкриває створений Excel-файл;
* додає діаграму на той самий аркуш;
* зберігає файл з уже готовим візуальним звітом.

Після цього файл можна відкрити в Excel і побачити діаграму.

---

## ✅ Результат

У результаті виконання цього етапу ви отримуєте:

* Excel-файл з аналізом витрат;
* автоматично створену діаграму;
* приклад реального використання Python для повсякденних задач.

---

## 💬 Для обговорення на уроці

* Чим це краще за ручне створення діаграми в Excel?
* Що буде, якщо додати нову категорію?
* Де ще можна використати таку автоматизацію?

---

> 🧠 **Висновок:** Python — це інструмент для автоматизації рутинних задач
