# Структура даних Stack (Стек)

## 1. Що таке стек?

Стек (Stack) — це структура даних, яка працює за принципом:

LIFO (Last In – First Out)  
Останній доданий елемент — перший видаляється.

Уявіть стопку тарілок:
- Додати тарілку можна тільки зверху
- Забрати можна тільки верхню

---

## 2. Основні операції стеку

- push(x)  → додати елемент у стек
- pop()    → видалити верхній елемент
- peek()   → подивитися на верхній елемент
- is_empty() → перевірити, чи стек порожній

---

## 3. На якій структурі даних побудований стек у Python?

У Python стек найчастіше реалізують на основі:

### 🔹 Списку (list)

Список у Python — це динамічний масив.

Методи:
- append()  → додає елемент в кінець
- pop()     → видаляє останній елемент

Оскільки стек працює тільки з "верхом",
а кінець списку — це і є "верх",
то список ідеально підходить для реалізації стеку.

---

## 4. Чому це ефективно?

Операції:
- append()
- pop()

працюють за O(1)

Тобто виконуються за константний час.

---

## 5. Де використовується стек?

- Виклик функцій (call stack)
- Перевірка дужок
- Скасування дій (undo/redo)
- DFS (обхід графів)
- Парсинг виразів

---

## 6. Візуалізація

push(10)
push(20)
push(30)

Стек:
| 30 |
| 20 |
| 10 |

pop() → поверне 30

---

## 7. Важливо

Стек — це НЕ окрема структура в Python.
Це логічна модель поведінки, яку ми реалізуємо через list або через власний клас.

 Чому для стеку в Python зазвичай НЕ використовують Linked List?


**Stack — це модель поведінки (LIFO), а не конкретна структура даних.**

Тобто стек — це спосіб роботи з даними:
> останній доданий елемент видаляється першим.

Його можна реалізувати різними структурами.

---

## Чому в Python використовують `list`?

- `list.append()` і `list.pop()` працюють за O(1)
- `list` реалізований на C → працює швидко
- код простий і зрозумілий
- не потрібно створювати додаткові обʼєкти

```python
stack.append(x)  # push
stack.pop()      # pop