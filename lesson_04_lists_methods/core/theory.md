# 📚 Теорія: Python List vs Linked List

## 1. Python list (динамічний масив)

List у Python --- це динамічний масив.

### Властивості:

-   Доступ по індексу: O(1)
-   Вставка в середину: O(n)
-   Видалення з середини: O(n)

### Основні методи:

-   append()
-   insert()
-   remove()
-   pop()
-   sort()
-   reverse()
-   count()
-   index()

------------------------------------------------------------------------

## 2. Проблема масиву

При вставці в середину всі елементи зсуваються.

\[10\]\[20\]\[30\]\[40\]\
↓\
\[10\]\[20\]\[25\]\[30\]\[40\]

------------------------------------------------------------------------

## 3. Linked List (зв'язний список)

Кожен вузол зберігає: - value - next (посилання на наступний)

Схема:

\[10 \| \* \] → \[20 \| \* \] → \[30 \| None\]

### Особливості:

-   Немає індексів
-   Доступ лише послідовний
-   Доступ до елемента: O(n)
-   Вставка на початок: O(1)

------------------------------------------------------------------------

## 4. Порівняння

  Операція             list   linked list
  -------------------- ------ -------------
  Доступ по індексу    O(1)   O(n)
  Вставка в середину   O(n)   O(n)
