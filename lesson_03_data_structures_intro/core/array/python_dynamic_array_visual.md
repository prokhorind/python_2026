# Python `list` під капотом — візуально

---

## 1️⃣ Стартовий приклад

```python
a = [10, 20, 30]
```

---

## 2️⃣ Що таке змінна `a`

```text
a
│
│  (reference)
▼0x1000  ──▶  LIST object
┌──────────────────────────────┐
│        list object           │
│   (dynamic array container)  │
└──────────────────────────────┘
```

🔹 `a` → посилання на **обʼєкт list**

---

## 3️⃣ list всередині (масив комірок)

```text
LIST  0x1000
┌───────┬───────┬───────┐
│ 0x2000│ 0x2010│ 0x2020│   ← array of references
└───────┴───────┴───────┘
   │        │        │
   ▼        ▼        ▼
  10       20       30   ← int objects
```

🔹 Кожна комірка — **посилання**  
🔹 Усі комірки **однакового розміру**

---

## 4️⃣ Посилання ведуть на обʼєкти

```text
[ 0x2000 ] ───▶ 10
[ 0x2010 ] ───▶ 20
[ 0x2020 ] ───▶ 30
```

🔹 `list` зберігає **НЕ значення**  
🔹 `list` зберігає **посилання на обʼєкти**

---

## Чому доступ за індексом O(1)

```text
address(i) = base_address + i * element_size
```

```text
┌──────┬──────┬──────┬──────┐
│ ptr  │ ptr  │ ptr  │ ptr  │
└──────┴──────┴──────┴──────┘
   0      1      2      3
```

🔹 `base_address` → адреса комірки `0`  
🔹 `element_size` → розмір **посилання**  
🔹 Арифметика адрес → **O(1)**

---

## Зміна елемента

```python
a[1] = 999
```

```text
Було:  [ 0x2010 ] ─▶ 20
Стало: [ 0x2010 ] ─▶ 999
```

🔹 Змінюється **посилання**  
🔹 Масив не рухається

---

## Видалення елемента (`del`)

```python
del a[1]
```

```text
Було:   [ 0x2000 ][ 0x2010 ][ 0x2020 ]
Індекси   0         1               2

Стає:   [ 0x2000 ][ 0x2020 ]
Індекси   0            1
```

🔹 Усі праві елементи **зсуваються вліво**  
🔹 Операція **O(n)**

---

## Ключова ідея (запамʼятати)

> Python `list` — це  
> **масив однакових комірок**,  
> де кожна комірка зберігає **посилання на обʼєкт**.

---

