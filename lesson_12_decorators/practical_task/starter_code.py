# Практичні завдання: Декоратори
# Заповни пропущені частини коду

print("🎓 Практичні завдання з декораторів")

# ==================== ЗАВДАННЯ 1: Простий декоратор логування ====================
print("\n📝 ЗАВДАННЯ 1: Простий декоратор логування")

def log_function(func):
    """Декоратор для логування викликів функцій"""
    def wrapper(*args, **kwargs):
        # TODO: Виведи повідомлення про початок функції
        print(f"Починаю {func.__name__}")
        
        # TODO: Виклич оригінальну функцію
        result = # TODO: виклич func з аргументами
        
        # TODO: Виведи повідомлення про завершення функції
        print(f"Завершую {func.__name__}")
        
        return result
    return wrapper

# TODO: Застосуй декоратор @log_function до функції say_hello
def say_hello(name):
    """Вітає користувача"""
    message = f"Привіт, {name}!"
    print(message)
    return message

# TODO: Застосуй декоратор @log_function до функції calculate_sum
def calculate_sum(a, b):
    """Додає два числа"""
    result = a + b
    print(f"Сума {a} + {b} = {result}")
    return result
    return result

print("Тестування декоратора логування:")
say_hello("Іван")
print()
calculate_sum(5, 3)

# ==================== ЗАВДАННЯ 2: Декоратор з підрахунком ====================
print("\n🔢 ЗАВДАННЯ 2: Декоратор з підрахунком")

def count_calls(func):
    """Декоратор для підрахунку викликів функції"""
    def wrapper(*args, **kwargs):
        # TODO: Збільш лічильник викликів
        wrapper.count += 1
        
        # TODO: Виведи повідомлення про кількість викликів
        print(f"Функція {func.__name__} викликана {wrapper.count} раз(ів)")
        
        # TODO: Виклич оригінальну функцію
        return # TODO: виклич func з аргументами
    
    # TODO: Ініціалізуй лічильник
    wrapper.count = # TODO: встанови початкове значення 0
    return wrapper

# TODO: Застосуй декоратор @count_calls до функції greet_student
def greet_student(name):
    """Вітає студента"""
    message = f"Привіт, студенте {name}!"
    print(message)
    return message

print("Тестування декоратора підрахунку:")
greet_student("Марія")
greet_student("Петро")
greet_student("Анна")

# ==================== ЗАВДАННЯ 3: Декоратор для перевірки ====================
print("\n✅ ЗАВДАННЯ 3: Декоратор для перевірки")

def check_positive(func):
    """Декоратор для перевірки позитивних чисел"""
    def wrapper(*args, **kwargs):
        # TODO: Перевір чи всі числові аргументи позитивні
        for arg in args:
            if isinstance(arg, (int, float)) and arg <= 0:
                print("❌ Помилка: всі числа повинні бути позитивними!")
                return None
        
        # TODO: Якщо всі числа позитивні, виклич функцію
        return # TODO: виклич func з аргументами
    return wrapper

# TODO: Застосуй декоратор @check_positive до функції calculate_area
def calculate_area(width, height):
    """Обчислює площу прямокутника"""
    area = width * height
    print(f"Площа: {area}")
    return area

# TODO: Застосуй декоратор @check_positive до функції calculate_average
def calculate_average(a, b, c):
    """Обчислює середнє арифметичне трьох чисел"""
    average = (a + b + c) / 3
    print(f"Середнє: {average:.1f}")
    return average

print("Тестування декоратора перевірки:")
print("Тест 1 (позитивні числа):")
result1 = calculate_area(5, 3)
print(f"Результат: {result1}")

print("\nТест 2 (негативне число):")
result2 = calculate_area(-5, 3)
print(f"Результат: {result2}")

print("\nТест 3 (позитивні числа):")
result3 = calculate_average(8, 9, 10)
print(f"Результат: {result3}")

print("\nТест 4 (нуль):")
result4 = calculate_average(0, 5, 10)
print(f"Результат: {result4}")

print("\n🎉 Всі завдання завершено! Молодець!")